# Pulse Reference Screenshots

Add the following three screenshots to this directory and name them exactly:

- `pulse-default.png` — The full "Today's Pulse" card grid (Modern style). Shows: period-grouped rows (MTD, QTD, YTD), 5-column card grid, each card with metric name + ⓘ + ··· menu, period label + certified dot, red % change above the value, large bold value, delta text, sparkline, and colored status bar at the bottom. Dark left nav with Cockpit / Pulse / Workspace.

- `pulse-ban.png` — BAN layout. Shows: 2-column full-width cards with period label (gray), bold metric name + certified badge, enormous value (~3.5rem), red percentage + delta text. No sparkline. Clean white cards, minimal chrome.

- `pulse-card.png` — Card layout. Shows: 2-column cards with period label, bold metric name, large value, red/green change line, full-width sparkline with confidence band fill and endpoint dot + vertical reference line, AI insight sentence below the sparkline with bold metric name and contributor breakdown.

## Source Documentation
- Embedding API — Pulse: https://help.tableau.com/current/api/embedding_api/en-us/docs/embedding_api_pulse.html
- Embedding API — Pulse Theming: https://help.tableau.com/current/api/embedding_api/en-us/docs/embedding_api_pulse_theme.html
